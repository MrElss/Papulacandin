# Core analysis stack for the Papulacandin / FKS1 serum-gap pipeline.
#
# These cover phases 0-9 of analysis/ that run on a normal workstation
# (data curation, 2D SAR, generative design, QM-descriptor parsing,
# retrospective statistics). Versions are pinned to the set validated in CI
# (see .github/workflows/ci.yml and DATA_PROVENANCE.md); the ordered pipeline
# in analysis/README.md and the tests/ smoke suite pass against them.
#
# Install:  python3 -m pip install -r requirements.txt

pandas==3.0.3
numpy==2.4.6
scipy==1.17.1
scikit-learn==1.9.0
matplotlib==3.11.0
joblib==1.5.3
rdkit==2026.3.3          # provides Contrib SA_Score (sascorer) used by phase5
pytest==8.4.2            # test runner for tests/

# Optional / heavier dependencies are NOT listed here — see
# requirements-optional.txt for the docking + slide-deck extras, and note that
# CREST, xtb and Gaussian are external binaries that are not pip-installable.
