# Python
__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/

# Environments
.venv/
venv/
env/

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini

# Large / regenerable model & compute artifacts (keep source + small results;
# add specific paths as the new pipeline defines them)
*.ckpt
*.pt
*.joblib

# Downloaded third-party training data (regenerable; the pipeline re-fetches it)
stage1b/data/
